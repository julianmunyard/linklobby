---
phase: 06-advanced-cards
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "@air/react-drag-to-select is installed"
    - "Package version is compatible"
  artifacts:
    - path: "package.json"
      provides: "@air/react-drag-to-select dependency"
      contains: "@air/react-drag-to-select"
  key_links: []
---

<objective>
Install @air/react-drag-to-select for box selection functionality.

Purpose: Adds the library needed for marquee/box selection of multiple cards on desktop.
Output: Package installed and ready for use in multi-select components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-advanced-cards/06-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @air/react-drag-to-select</name>
  <files>package.json, package-lock.json</files>
  <action>
Install the box selection library:

```bash
npm install @air/react-drag-to-select
```

This library provides:
- useSelectionContainer hook for creating selection boxes
- boxesIntersect helper for detecting which items are in selection
- Configurable selection box styling
- 60fps performance for smooth selection experience

Verify the installation by checking package.json for the new dependency.
  </action>
  <verify>`npm list @air/react-drag-to-select` shows the package is installed</verify>
  <done>@air/react-drag-to-select is in dependencies and installed</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript Types</name>
  <files>N/A (verification only)</files>
  <action>
Verify TypeScript can resolve the library types:

1. Create a temporary test file or just run tsc to verify types resolve
2. The library should have built-in TypeScript definitions
3. If types are missing, we would need @types/air__react-drag-to-select (unlikely)

Run: `npx tsc --noEmit` to verify no type errors
  </action>
  <verify>`npx tsc --noEmit` passes without type resolution errors for the new package</verify>
  <done>TypeScript can import from @air/react-drag-to-select without errors</done>
</task>

</tasks>

<verification>
- `npm list @air/react-drag-to-select` shows package installed
- `npx tsc --noEmit` passes
- Can import { useSelectionContainer } from "@air/react-drag-to-select"
</verification>

<success_criteria>
- Package installed successfully
- No version conflicts
- TypeScript types available
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-cards/06-04-SUMMARY.md`
</output>
